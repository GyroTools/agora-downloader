# Copy this file to .env (which is gitignored) and fill in your real key.
# Used by .vscode/launch.json's "envFile" so you don't have to enter the
# API key on every debug launch - it's read via the --api-key flag's
# AGORA_API_KEY env var binding (see main.go).
AGORA_API_KEY=
